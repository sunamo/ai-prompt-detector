import * as vscode from 'vscode';
import * as fs from 'fs';
import * as path from 'path';
import { state } from '../state';

// Legacy removed (do not recreate): src/utils/logging.ts replaced by src/logger.ts. File intentionally left as comment only.
